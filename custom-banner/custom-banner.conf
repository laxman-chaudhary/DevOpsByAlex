# /etc/custom-banner.conf

# Toggle banner visibility
QUIET_MODE=false

# Toggle logging JSON output to /var/log/custom-banner.log
ENABLE_LOGGING=true

# Toggle Docker stats section (disable if Docker isn't used)
SHOW_DOCKER=true

# Optional environment label to display (e.g., dev, prod, staging)
ENVIRONMENT=dev
